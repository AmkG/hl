; test for lambdas

(test "straight lambda"
  (<axiom>lambda () nil) ((check-vars 2)
                          (closure 0
                            (check-vars 2)
                            (lit-nil)
                            (continue))
                          (continue)))
